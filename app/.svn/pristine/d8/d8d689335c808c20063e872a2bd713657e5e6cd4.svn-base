'use strict';

var app = angular.module('cwwRedstone', ['cwwRedstone.directives', 'cwwRedstone.services']);
/*
app.config(['$routeProvider', function($routeProvider) {
    $routeProvider.
      when('/', {
        resolve: {
          courseOutline: 'CourseOutline'
        },
        controller: 'LoaderCtrl',
        templateUrl:'./views/main.html'
      })
      .when('/content/:id', {
        controller: 'MainCtrl',
        templateUrl:'./views/main.html'
      })
      .otherwise({redirectTo:'/'});
}]);


// COURSE OUTLINE LOADER CONTROLLER
app.controller('LoaderCtrl', ['$scope', 'courseOutline', 
  function($scope, courseOutline) {
    console.log(courseOutline);
}]);

// MAIN CONTROLLER
app.controller('MainCtrl', ['$scope', '$routeParams', 
  function($scope, $routeParams) {
    $scope.screenId = $routeParams.id;
    console.log($scope.screenId, $scope.courseOutline);
  }]);
  */